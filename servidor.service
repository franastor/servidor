[Unit]
Description=Servidor Flask
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/servidor
Environment="PATH=/root/servidor/venv/bin"
ExecStart=/root/servidor/venv/bin/python3 app.py
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target 